import styled from 'styled-components'

export const InputBoxWrapper = styled.label`
  position: relative;
  input {
    border: none;
    padding: 0 15px;
    height: 40px;
  }
  button {
    position: absolute;
    top: 7.5px;
    right: 5px;
  }
`
